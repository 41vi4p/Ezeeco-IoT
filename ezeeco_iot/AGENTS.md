# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

For every changes, add the docs properly in the docs/CHANGELOG.md file. make sure all the changes are tracked properly. Also go on incrementing the app version for all the changes made.
